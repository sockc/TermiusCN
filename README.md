# TermiusCN

LSPosed runtime text translation module for Termius (`com.server.auditor.ssh.client`).

## Version
- 0.3.0: fixes Xposed stub signature mismatch that caused `NoSuchMethodError` at startup.
